Use $token-pilot. This is a forward test; do not execute the proposed work. Treat the following as the complete runtime capability inventory: only gpt-5.6-sol with low reasoning is confirmed callable; Fast is unavailable; full model discovery is unavailable. Request: Summarize three local design documents and produce one decision memo. Produce only the pre-confirmation response that the skill requires.
