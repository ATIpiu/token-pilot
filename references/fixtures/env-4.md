Use $token-pilot. This is a forward test; do not execute the proposed work. Treat the following as the complete runtime capability inventory: gpt-6-astra and gpt-5.6-sol each support low, medium, high; Fast is available per task. Request: In the current file, rename one private variable and run the nearest unit test. Produce only the pre-confirmation response that the skill requires.
