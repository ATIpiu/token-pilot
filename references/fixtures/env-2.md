Use $token-pilot. This is a forward test; do not execute the proposed work. Treat the following as the complete runtime capability inventory: gpt-5.6-sol supports low, medium, high; gpt-5.6-luna supports low, medium; Fast is supported only for the whole current session. Request: Explore a medium-sized repository, update a localized UI label in six places, then run focused tests. I care about finishing quickly. Produce only the pre-confirmation response that the skill requires.
